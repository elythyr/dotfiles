#
# Environment variables are accessible but they must be defined before sxhkd
# starts. Since it's started by the X server it's done through a login shell so
# the best place to put them is the .profile file which should be loaded by
# most shells
#

# Applications {{{

alt + F1
    keybindings

# Show the powermenu
alt + shift + BackSpace
    powermenu

# Reload sxhkd configuration
alt + Escape
    pkill -USR1 -x sxhkd

# Start a new terminal
alt + {_,shift + }Return
    {samedir,$TERMINAL}

# Program launcher
alt + @space
    rofi -show run

# Applications shortcuts
alt + o ; {b,w}
    { \
        $BROWSER, \
        vimwiki, \
    }

# Lock the screen
super + l
    loginctl lock-session

# }}}

# Media keys {{{

# Control the monitor brightness
{_,shift + }XF86MonBrightness{Down,Up}
    brightnessctl set {5,10}%{-,+}

# Control the volume
XF86Audio{Lower,Raise}Volume{_, + shift}
    pactl set-sink-volume @DEFAULT_SINK@ {-,+}{5,10}% >/dev/null 2>&1 || notify-send "Impossible to change the volume, check you sxhkd configuration."

# Mute
XF86AudioMute
    pactl set-sink-mute @DEFAULT_SINK@ toggle >/dev/null 2>&1 || notify-send "Impossible to change the volume, check you sxhkd configuration."

# }}}

# vim: ts=4 sw=4 et fdm=marker
